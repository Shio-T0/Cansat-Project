# RpiMotorLib configuration
# backend: null | rpigpio | lgpio
#   null    = auto-detect (recommended)
#   rpigpio = force RPi.GPIO / rpi-lgpio
#   lgpio   = force native lgpio

[gpio]
backend = null

